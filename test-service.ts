import { AccountsService } from './modules/m05-account-intelligence/services/accounts.service'; async function run() { const service = new AccountsService(); const res = await service.getAccounts('tenant', { board_slug: 'demo', period: 'Last quarter' }, 'userId123', 'sales_manager'); console.log('Total:', res.total, 'Accounts array length:', res.accounts.length); } run().catch(console.error);
