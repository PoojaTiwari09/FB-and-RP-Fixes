async function run() { const res = await fetch('http://localhost:3001/api/v1/platform/auth/login', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ email: 'manager@example.com', password: 'password123' }) }); const data = await res.json(); if (!data.token) { console.error('Login failed:', data); return; } const token = data.token; const tenantId = data.tenantId || '00000000-0000-0000-0000-000000000001'; const accountsRes = await fetch('http://localhost:3001/api/manager/revenue/accounts?board_slug=demo&page=1&size=20', { headers: { 'x-tenant-id': tenantId, 'authorization': 'Bearer ' + token } }); const accountsData = await accountsRes.json(); console.log('Manager API accounts:', JSON.stringify(accountsData, null, 2)); } run().catch(console.error);
