import { createM05DbClient } from './modules/m05-account-intelligence/database/m05-db.client'; import { BoardsService } from './modules/m05-account-intelligence/services/boards.service'; async function run() { const p = createM05DbClient(); const s = new BoardsService(p as any); try { const col = await s.addColumn('demo', { field_key: 'custom_score', label: 'Custom Score', column_type: 'supplementary', order: 10, width: 120, sortable: true, editable: false, visible_to_roles: ['manager', 'admin'] }); console.log('Created!', col); } catch (e) { console.error('Error!', e); } } run().catch(console.error);
