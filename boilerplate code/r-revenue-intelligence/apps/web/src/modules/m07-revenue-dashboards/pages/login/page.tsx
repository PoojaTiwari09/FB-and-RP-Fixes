export default function LoginPage() {
  return (
    <main className="shell">
      <section className="card">
        <h1>Login</h1>
        <p>JWT authentication is handled by the NestJS platform-core auth module.</p>
      </section>
    </main>
  );
}
