# Legacy Next.js entry — not used at runtime

Canonical M03 UI is the **Vite** app:

- `index.html` → `src/main.jsx` → `src/App.jsx`
- `src/pages/*` for routing

The `app/page.tsx` file is a vendored Next.js artifact kept for reference only. Do not add it to production builds.
