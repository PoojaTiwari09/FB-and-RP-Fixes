/**
 * Server-only re-exports for Next.js API routes (Turbopack-safe static imports).
 */
export {
  generateLiveAssistInsights,
  analyzeCompetitorThreat,
  generateStrategicSummary,
} from '../live-assist/services/gemini.js';
