export declare const pinoHttpOptions: {
    pinoHttp: {
        level: string;
        transport: {
            target: string;
            options: {
                colorize: boolean;
            };
        };
        redact: string[];
    };
};
